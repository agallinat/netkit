% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_CCDF.R
\name{plot_CCDF}
\alias{plot_CCDF}
\title{Plot Complementary Cumulative Degree Distribution (CCDF)}
\usage{
plot_CCDF(
  graph,
  keep_direction = TRUE,
  remove_singles = FALSE,
  show_PL = TRUE,
  PL_exponents = c(2, 3),
  colors = c("#000831", "#e41a1c", "darkgreen", "#9c52f2", "#b8b8ff"),
  label.size = 12
)
}
\arguments{
\item{graph}{An \code{igraph} object representing the network to analyze or a
data frame containing a symbolic edge list in the first two columns. Additional
columns are considered as edge attributes.}

\item{keep_direction}{Logical. Only for directed graphs. If \code{TRUE}, CCDF curves are drawn for 'in'-dgree,
'out'-degree, and 'all'-degree distributions. \code{FALSE} to ignore directionality.}

\item{remove_singles}{Logical. If \code{TRUE}, nodes with degree 0 are removed from the graph
before computing the CCDF. Default is \code{FALSE}.}

\item{show_PL}{Logical. If \code{TRUE}, overlays theoretical power-law reference lines of the form
\eqn{P(K > k) \sim k^{-\gamma}}. Default is \code{TRUE}.}

\item{PL_exponents}{Numeric vector. The \eqn{\gamma} exponents for the power-law curves. Default is \code{c(2, 3)}.}

\item{colors}{Optional character vector. Custom colors for the graph curve and power-law lines.
If \code{NULL}, default colors are used.}

\item{label.size}{Numeric. Font size for axis labels and theme. Passed to \code{theme_minimal}.}
}
\value{
A \code{ggplot2} object showing the CCDF of node degrees on a log-log scale.
}
\description{
This function plots the complementary cumulative distribution function (CCDF)
of node degrees in a network and optionally overlays power-law reference curves.
}
\examples{
\dontrun{
library(igraph)
g <- sample_pa(1000)
plot_CCDF(g, remove_singles = TRUE)
}

}
